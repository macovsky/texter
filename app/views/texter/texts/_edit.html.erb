<%= form_for @text, url: text_path(@text), remote: true, method: :put do |f| %>
  <%= f.hidden_field :tag_type %>
  <%= f.text_area :body %>
  <%= f.submit Texter.t('submit', default: 'Сохранить'), data: { disable_with: Texter.t('disable_with', default: '✔ Сохраняется...') } %>
<% end %>
